\documentclass{article}
\usepackage{a4wide}
\usepackage{amssymb}

\begin{document}

$$\frac{\texttt{min}+\texttt{max}}{2} + \frac{\texttt{max}-\texttt{min}}{2}\epsilon_1$$

$$\hat{x} = \alpha_0^x + \sum_{i=1}^n \alpha_i^x \epsilon_i$$
$$\hat{y} = \beta_0^y + \sum_{i=1}^n \beta_i^y \epsilon_i$$
$$\widehat{x+y} = (\alpha_0^x + \beta_0^y) + \sum_{i=1}^n (\alpha_i^x + \beta_i^y) \epsilon_i$$
$$\widehat{x-y} = (\alpha_0^x - \beta_0^y) + \sum_{i=1}^n (\alpha_i^x - \beta_i^y) \epsilon_i$$
$$\widehat{-x} = (-\alpha_0^x) + \sum_{i=1}^n (-\alpha_i^x) \epsilon_i$$


$$\begin{array}{rcl}
\widehat{x \times y} & = & {\displaystyle (\alpha_0^x \times \beta_0^y)
    + \sum_{i=1}^n (\beta_0^y \times \alpha_i^x + \alpha_0^x \times \beta_i^y) \epsilon_i
    + \sum_{i=1}^n \sum_{j=1,j \neq i}^n (\alpha_i^x \times \beta_j^y) \epsilon_i\epsilon_j
    + \sum_{i=1}^n (\alpha_i^x \times \beta_i^y) \epsilon_i^2}\end{array}$$
$$\begin{array}{rcl}\displaystyle
\widehat{x \times y} & = & {\displaystyle (\alpha_0^x \times \beta_0^y) + \frac{1}{2} \times \left(\sum_{i=1}^n (\alpha_i^x \times \beta_i^y)\right)}\\
& & {\displaystyle + \sum_{i=1}^n (\beta_0^y \times \alpha_i^x + \alpha_0^x \times \beta_i^y) \epsilon_i}\\
& & {\displaystyle + \left(\sum_{i=1}^n \sum_{j=i+1}^n |\alpha_i^x \times \beta_j^y + \alpha_j^x \times \beta_i^y|
    + \frac{1}{2} \times \left(\sum_{i=1}^n |\alpha_i^x \times \beta_i^y|\right)\right) \epsilon_{\texttt{\scriptsize new}}}
\end{array}$$
$$\alpha_0^x \times \beta_0^y + \sum_{i=1}^n \frac{1}{2} (\alpha_i^x \times \beta_i^y)$$
$$
\sum_{i=1}^n \sum_{j=i+1}^n |\alpha_i^x \times \beta_j^y + \alpha_j^x \times \beta_i^y|
+ \sum_{i=1}^n \frac{1}{2} |\alpha_i^x \times \beta_i^y|
$$
$$\alpha_0^x \times \beta_i^y + \beta_0^y \times \alpha_i^x$$

$$\widehat{\texttt{value} \times x} = (\texttt{value} \times \alpha_0^x)
    + \sum_{i=1}^n (\texttt{value} \times \alpha_i^x) \epsilon_i$$
$$\widehat{x / \texttt{value}} = \frac{\alpha_0^x}{\texttt{value}}
    + \sum_{i=1}^n \frac{\alpha_i^x}{\texttt{value}} \epsilon_i$$


$$\begin{array}{rcl}
\widehat{1 / x} & = & {\displaystyle \frac{1}{\alpha_0^x} \times \frac{1}{\displaystyle 1
    + \sum_{i=1}^n \frac{\alpha_i^x}{\alpha_0^x} \epsilon_i}}\\
& = & {\displaystyle\frac{1}{\alpha_0^x} \times \frac{1}{1 + t} \texttt{ with }
   t = \sum_{i=1}^n \frac{\alpha_i^x}{\alpha_0^x} \epsilon_i}\\
& = & {\displaystyle \frac{1}{\alpha_0^x} \times \left(1 - t + \frac{t^2}{1+t}\right)}\\
& = & {\displaystyle \frac{1}{\alpha_0^x} \times \left(1 - t
    + [0, \frac{(-t_{\texttt{\scriptsize min}})^2}{1+t_{\texttt{\scriptsize min}}}]\right)}\\
& = & {\displaystyle \frac{1}{\alpha_0^x} \times \left(1 - t
    + \frac{1}{2}\times\frac{(-t_{\texttt{\scriptsize min}})^2}{1+t_{\texttt{\scriptsize min}}}
    + \frac{1}{2}\times\frac{(-t_{\texttt{\scriptsize min}})^2}{1+t_{\texttt{\scriptsize min}}}\epsilon_{\texttt{\scriptsize new}}\right)}\\
& = & {\displaystyle \frac{1}{\alpha_0^x} \times \left(1
    + \frac{1}{2}\times\frac{\displaystyle \left(\sum_{i=1}^n \left|\frac{\alpha_i^x}{\alpha_0^x}\right|\right)^2}
      {\displaystyle 1-\sum_{i=1}^n \left|\frac{\alpha_i^x}{\alpha_0^x}\right|}
    - \left(\sum_{i=1}^n \frac{\alpha_i^x}{\alpha_0^x} \epsilon_i\right)
    + \frac{1}{2}\times\frac{\displaystyle \left(\sum_{i=1}^n \left|\frac{\alpha_i^x}{\alpha_0^x}\right|\right)^2}
      {\displaystyle 1-\sum_{i=1}^n \left|\frac{\alpha_i^x}{\alpha_0^x}\right|}\epsilon_{\texttt{\scriptsize new}}\right)}\\
& = & {\displaystyle \frac{1}{(\alpha_0^x)^2} \times \left(\alpha_0^x
    + \frac{1}{2}\times\frac{\displaystyle \texttt{sign}(\alpha_0^x)\times\left(\sum_{i=1}^n \left|\alpha_i^x\right|\right)^2}
      {\displaystyle \left|\alpha_0^x\right|-\sum_{i=1}^n \left|\alpha_i^x\right|}
    - \left(\sum_{i=1}^n \alpha_i^x \epsilon_i\right)
    + \frac{1}{2}\times\frac{\displaystyle \texttt{sign}(\alpha_0^x)\times\left(\sum_{i=1}^n \left|\alpha_i^x\right|\right)^2}
      {\displaystyle |\alpha_0^x| -\sum_{i=1}^n \left|\alpha_i^x\right|}\epsilon_{\texttt{\scriptsize new}}\right)}
\end{array}$$

$$\begin{array}{rcl}
\widehat{x / y} & = & {\displaystyle \frac{\alpha_0^x}{\beta_0^y} \times
    \frac{\displaystyle 1 + \sum_{i=1}^n \frac{\alpha_i^x}{\alpha_0^x} \epsilon_i}
         {\displaystyle 1 + \sum_{i=1}^n \frac{\beta_i^y}{\beta_0^y} \epsilon_i}}\\
& = & {\displaystyle \frac{\alpha_0^x}{\beta_0^y} \times \frac{1+t}{1+u} \texttt{ with }
    t = \sum_{i=1}^n \frac{\alpha_i^x}{\alpha_0^x} \epsilon_i \texttt{ and }
    u = \sum_{i=1}^n \frac{\beta_i^y}{\beta_0^y} \epsilon_i}\\
& = & {\displaystyle \frac{\alpha_0^x}{\beta_0^y} \times \left(1 + \frac{t-u}{1+u}\right)}\\
& = & {\displaystyle \frac{\alpha_0^x}{\beta_0^y}
    + \frac{\alpha_0^x}{\beta_0^y} \times \frac{\displaystyle
      \sum_{i=1}^n \left(\frac{\alpha_i^x}{\alpha_0^x} - \frac{\beta_i^y}{\beta_0^y}\right) \epsilon_i}
    {\displaystyle 1 + \sum_{i=1}^n \frac{\beta_i^y}{\beta_0^y} \epsilon_i}}\\
& = & {\displaystyle \frac{\alpha_0^x}{\beta_0^y}
    + \left(\sum_{i=1}^n \left(\frac{\alpha_i^x\beta_0^y - \alpha_0^x\beta_i^y}{\beta_0^y}\right) \epsilon_i \right)
    \times \widehat{1 / y}}
\end{array}$$

\begin{itemize}
\item centralSquare = $(\alpha_0^x)^2$\\
      highLevelCentralSquare = $\texttt{sulp}(\texttt{centralSquare})$
\item max = ${\displaystyle \sum_{i=1}^n |\alpha_i^x|}$\\
      highLevelMax =  ${\displaystyle \sum_{i=2}^n \texttt{sulp}(\sum_{j=1}^{i} |\alpha_j^x|)}$
\item centralDiff = $|\alpha_0^x| \times (|\alpha_0^x| - \texttt{max}) = 
      |\alpha_0^x| \times \left(|\alpha_0^x| - {\displaystyle \sum_{i=1}^n |\alpha_i^x|}\right)$\\
      highLevelCentralDiff = $(\texttt{highLevelMax} + \texttt{sulp}(|\alpha_0^x| - \texttt{max}))
        \times |\alpha_0^x| + \texttt{sulp}(\texttt{centralDiff})$
\item maxSquareDivideDiff = ${\displaystyle \frac{\texttt{max}^2}{\texttt{centralDiff}}
      = \frac{\displaystyle \texttt{sign}(\alpha_0^x)\times\left(\sum_{i=1}^n |\alpha_i^x|\right)^2}
             {\alpha_0^x \times \left(|\alpha_0^x| - {\displaystyle \sum_{i=1}^n |\alpha_i^x|}\right)}}$\\
      highLevelMaxSquare = $2 \times \texttt{max} \times \texttt{highLevelMax}
        + \texttt{highLevelMax}^2 + \texttt{sulp}(\texttt{max}^2)$\\
      highLevelMaxSquareDivideDiff\\
      = ${\displaystyle \left(\frac{\texttt{max}^2 + \texttt{highLevelMaxSquare}}
        {\texttt{centralDiff} - \texttt{highLevelCentralDiff}}
        - \frac{\texttt{max}^2}{\texttt{centralDiff}}\right) + \texttt{sulp}(\texttt{maxSquareDivideDiff})}$\\
      = ${\displaystyle
        \frac{(\texttt{max}^2 + \texttt{highLevelMaxSquare})\times\texttt{centralDiff}
          -\texttt{max}^2\times(\texttt{centralDiff} - \texttt{highLevelCentralDiff})}
          {\texttt{centralDiff}\times(\texttt{centralDiff} - \texttt{highLevelCentralDiff})}}$\\
        \hspace*{1in}$+ \texttt{sulp}(\texttt{maxSquareDivideDiff})$\\
      = ${\displaystyle
        \frac{\texttt{highLevelMaxSquare}\times\texttt{centralDiff}
          +\texttt{max}^2\times\texttt{highLevelCentralDiff}}
          {\texttt{centralDiff}\times(\texttt{centralDiff} - \texttt{highLevelCentralDiff})}
        + \texttt{sulp}(\texttt{maxSquareDivideDiff})}$
\item $\forall i \in [1, n]. \, $ coeff$_i$ = ${\displaystyle \frac{-\alpha_i^x}{\texttt{centralSquare}}
      = \frac{-\alpha_i^x}{(\alpha_0^x)^2}}$\\
      highLevelCoeff$_i$ = ${\displaystyle \frac{|\alpha_i^x|}{\texttt{centralSquare} - \texttt{highLevelCentralSquare}}
        - \frac{|\alpha_i^x|}{\texttt{centralSquare}} + \texttt{sulp}(\texttt{coeff}_i)}$\\
      = ${\displaystyle |\alpha_i^x| \times \frac{\texttt{highLevelCentralSquare}}{\texttt{centralSquare}
        \times (\texttt{centralSquare} - \texttt{highLevelCentralSquare})} + \texttt{sulp}(\texttt{coeff}_i)}$
\item newCentral = ${\displaystyle \frac{\displaystyle \frac{\texttt{maxSquareDivideDiff}}{2} + 1}
        {\alpha_0^x}
      = \frac{1}{2} \times \frac{\displaystyle \texttt{sign}(\alpha_0^x)\times\left(\sum_{i=1}^n |\alpha_i^x|\right)^2}
             {(\alpha_0^x)^2 \times \left(|\alpha_0^x| - {\displaystyle \sum_{i=1}^n |\alpha_i^x|}\right)}
          + \frac{1}{\alpha_0^x}}$\\
      highLevelNewCentral =\\
      ${\displaystyle \frac{\displaystyle \frac{\texttt{highLevelMaxSquareDivideDiff}}{2}
        + \texttt{sulp}(\frac{\texttt{maxSquareDivideDiff}}{2}) + \texttt{sulp}(\frac{\texttt{maxSquareDivideDiff}+1}{2})}
        {\alpha_0^x}}$\\
      \hspace*{1in}${\displaystyle + \texttt{sulp}(\texttt{newCentral})}$
\item newCoeff = ${\displaystyle \frac{1}{2} \times \frac{\texttt{maxSquareDivideDiff}}{\alpha_0^x}
      = \frac{1}{2} \times \frac{\displaystyle \texttt{sign}(\alpha_0^x)\times\left(\sum_{i=1}^n |\alpha_i^x|\right)^2}
             {(\alpha_0^x)^2 \times \left(|\alpha_0^x| - {\displaystyle \sum_{i=1}^n |\alpha_i^x|}\right)}}$\\
      highLevelNewCoeff = ${\displaystyle \frac{\displaystyle \frac{\texttt{highLevelMaxSquareDivideDiff}}{2}
        + \texttt{sulp}(\frac{\texttt{maxSquareDivideDiff}}{2})}
        {\alpha_0^x} + \texttt{sulp}(\texttt{newCoeff})}$
\item highLevelError = ${\displaystyle \texttt{highLevelNewCentral} + \sum_{i=1}^n\texttt{highLevelCoeff}_i}
      + \texttt{highLevelNewCoeff}$
\end{itemize}

$$\widehat{1 / x} = \frac{1}{(\alpha_0^x)^2} \times \left(\alpha_0^x
    + \frac{1}{2}\times\frac{\displaystyle \texttt{sign}(\alpha_0^x)\times\left(\sum_{i=1}^n \left|\alpha_i^x\right|\right)^2}
      {\displaystyle \left|\alpha_0^x\right|-\sum_{i=1}^n \left|\alpha_i^x\right|}
    - \left(\sum_{i=1}^n \alpha_i^x \epsilon_i\right)
    + \frac{1}{2}\times\frac{\displaystyle \texttt{sign}(\alpha_0^x)\times\left(\sum_{i=1}^n \left|\alpha_i^x\right|\right)^2}
      {\displaystyle |\alpha_0^x| -\sum_{i=1}^n \left|\alpha_i^x\right|}\epsilon_{\texttt{\scriptsize new}}\right)
$$

$$\widehat{x / y} = \frac{\alpha_0^x}{\beta_0^y}
    + \left(\sum_{i=1}^n \left(\frac{\alpha_i^x\beta_0^y - \alpha_0^x\beta_i^y}{\beta_0^y}\right) \epsilon_i \right)
    \times \widehat{1 / y}$$

$$\widehat{1 / y} = \texttt{sourceInverse} + \texttt{inverseNewCoeff} \times \epsilon'_{\texttt{\scriptsize\em new}}
   + \texttt{inverseHighLevel} \times \epsilon'_{\texttt{\scriptsize\em err}}$$
$$\sum_{i=1}^n \left(\frac{\alpha_i^x\beta_0^y - \alpha_0^x\beta_i^y}{\beta_0^y}\right) \epsilon_i
    = \texttt{this}^{\texttt{\scriptsize\em adj}} + \texttt{highLevelLinear} \times \epsilon''_{\texttt{\scriptsize\em err}}$$
$$\texttt{max} = \sum_{i=1}^n \left|\frac{\alpha_i^x\beta_0^y - \alpha_0^x\beta_i^y}{\beta_0^y}\right|
    = \max(\texttt{this}^{\texttt{\scriptsize\em adj}})$$
$$\texttt{maxSourceInverse} = = \max(\texttt{sourceInverse})$$
$$\begin{array}{rcl} \displaystyle
  \widehat{\texttt{this}^{\texttt{\scriptsize\em adj}} \times \texttt{sourceInverse}}
    & = & \texttt{this}^{\texttt{\scriptsize\em mult}} + \texttt{multNewCoeff} \times \epsilon'''_{\texttt{\scriptsize\em new}}
      + \texttt{multHighLevel} \times \epsilon'''_{\texttt{\scriptsize\em err}} \\
  & = & \displaystyle\left(\left(\sum_{i=1}^n \left(\frac{\alpha_i^x\beta_0^y - \alpha_0^x\beta_i^y}{\beta_0^y}\right) \epsilon_i\right)
      - \texttt{highLevelLinear} \times \epsilon''_{\texttt{\scriptsize\em err}}\right)\\
  &   & \displaystyle\times \left(\widehat{1 / y} - \texttt{inverseNewCoeff} \times \epsilon'_{\texttt{\scriptsize\em new}}
      - \texttt{inverseHighLevel} \times \epsilon'_{\texttt{\scriptsize\em err}}\right)
\end{array}
$$
$$\begin{array}{rcl} \displaystyle
  \widehat{\texttt{this}^{\texttt{\scriptsize\em adj}} \times \texttt{sourceInverse}}
    & = & \texttt{this}^{\texttt{\scriptsize\em mult}} + \texttt{multNewCoeff} \times \epsilon'''_{\texttt{\scriptsize\em new}}
      + \texttt{multHighLevel} \times \epsilon'''_{\texttt{\scriptsize\em err}} \\
  & = & \displaystyle\left(\left(\sum_{i=1}^n \left(\frac{\alpha_i^x\beta_0^y - \alpha_0^x\beta_i^y}{\beta_0^y}\right) \epsilon_i\right)
      - \texttt{highLevelLinear} \times \epsilon''_{\texttt{\scriptsize\em err}}\right)\\
  &   & \displaystyle\times \left(\widehat{1 / y} - \texttt{inverseNewCoeff} \times \epsilon'_{\texttt{\scriptsize\em new}}
      - \texttt{inverseHighLevel} \times \epsilon'_{\texttt{\scriptsize\em err}}\right)
\end{array}$$

$$\begin{array}{rcl} \displaystyle
  \widehat{x / y} & = & \displaystyle \frac{\alpha_0^x}{\beta_0^y}
    + \left(\sum_{i=1}^n \left(\frac{\alpha_i^x\beta_0^y - \alpha_0^x\beta_i^y}{\beta_0^y}\right) \epsilon_i \right)
    \times \widehat{1 / y}\\
  & = & \displaystyle  \frac{\alpha_0^x}{\beta_0^y} +
   \texttt{this}^{\texttt{\scriptsize\em mult}} + \texttt{multNewCoeff} \times \epsilon'''_{\texttt{\scriptsize\em new}}
      + \texttt{multHighLevel} \times \epsilon'''_{\texttt{\scriptsize\em err}}\\
  &   & \displaystyle + \texttt{ this}^{\texttt{\scriptsize\em adj}} \times  \left(\texttt{inverseNewCoeff} \times \epsilon'_{\texttt{\scriptsize\em new}}
      + \texttt{inverseHighLevel} \times \epsilon'_{\texttt{\scriptsize\em err}}\right)\\
  &   & \displaystyle + \texttt{ sourceInverse} \times \texttt{highLevelLinear} \times \epsilon''_{\texttt{\scriptsize\em err}}\\
  & = & \displaystyle \frac{\alpha_0^x}{\beta_0^y} +
   \texttt{this}^{\texttt{\scriptsize\em mult}} + \left(\texttt{multNewCoeff}
      + \texttt{max} \times \texttt{inverseNewCoeff}\right)\times \epsilon_{\texttt{\scriptsize\em new}}\\
  &   & \displaystyle + \left(\texttt{multHighLevel} + \texttt{max} \times \texttt{inverseHighLevel}\right.\\
  &   & \displaystyle \hspace*{1em}\left. + \texttt{ maxSourceInverse} \times \texttt{highLevelLinear}\right) \times \epsilon_{\texttt{\scriptsize\em err}}
\end{array}$$

$$\texttt{this}_{\texttt{\scriptsize\em equ}} = \widehat{x} = \alpha_0^x + \sum_{i=1}^n \alpha_i^x \epsilon_i$$
$$\texttt{constraint}_{\texttt{\scriptsize\em equ}} = \widehat{y} = \beta_0^y + \sum_{i=1}^n \beta_i^y \epsilon_i$$
$$\texttt{constraintNormalization} = \beta_0^y + \sum_{i=1}^n |\beta_i^y|$$
$$\texttt{constraintNormalization} = \beta_0^y - \sum_{i=1}^n |\beta_i^y|$$
$$\texttt{constraintSymbolCoefficient} = \gamma \times \frac{|\texttt{constraintNormalization}|}{2}$$
$$\texttt{constraintSymbol} = \epsilon_c = \frac{2\times\widehat{y} - \texttt{constraintNormalization}}{\texttt{constraintNormalization}}$$
$$\texttt{remainder}_{\texttt{\scriptsize\em equ}} = \sum_{i=1}^n r_i \epsilon_i$$
$$\widehat{x} = \gamma \times \frac{|\texttt{constraintNormalization}|}{2} \times \epsilon_c + r_0 + \sum_{i=1}^n r_i \epsilon_i$$
$$\texttt{remainder} = \sum_{i=1}^n |r_i|$$
$$\texttt{baseRemainder} = \sum_{i=1}^n |\beta_i^y|$$
$$\texttt{weightPositive} = \sum_{i=1}^n |\beta_i| \textrm{ if } \texttt{sign}(\gamma \times \beta_i^y) = \texttt{sign}(\alpha_i^x)$$
$$\texttt{weightAll} = \sum_{i=1}^n |\beta_i|$$
$$\texttt{matchAll} = \sum_{i=1}^n |\alpha_i^x|$$
$$\texttt{match} = \texttt{matchPositive} = |\gamma| \times \texttt{weightPositive} = \sum_{i=1}^n |\gamma \times \beta_i| \textrm{ if }
   \texttt{sign}(\gamma \times \beta_i^y) = \texttt{sign}(\alpha_i^x)$$
$$\texttt{matchNegative} = \sum_{i=1}^n |\gamma \times \beta_i| \textrm{ if }
   \texttt{sign}(\gamma \times \beta_i^y) \neq \texttt{sign}(\alpha_i^x)$$
$$\texttt{matchAll} + \texttt{matchNegative} = \texttt{matchPositive} - \texttt{matchNegative} + \texttt{remainder}$$
$$\texttt{matchAll} + 2 \times \texttt{matchNegative} = \texttt{matchPositive} + \texttt{remainder}$$

$$\frac{\texttt{matchPositive}}{\texttt{matchAll}} = \frac{\texttt{match}}{\texttt{matchAll}}$$ 
$$\frac{\texttt{matchNegative}}{\texttt{matchAll}} = \frac{\texttt{match} + \texttt{remainder} - \texttt{matchAll}}{2 \times \texttt{matchAll}}$$ 

$$\texttt{matchAll}^{\mbox{\scriptsize\em new}} \leftarrow \texttt{matchAll}^{\mbox{\scriptsize\em old}} + |\alpha_i^x|$$
$$\texttt{weightAll}^{\mbox{\scriptsize\em new}} \leftarrow \texttt{weightAll}^{\mbox{\scriptsize\em old}} + |\beta_i^x|$$
$$\epsilon_c^{\mbox{\scriptsize\em new}} \leftarrow \epsilon_c^{\mbox{\scriptsize\em old}} + \beta_i^y \times \frac{2}{\texttt{constraintNormalization}} \times \epsilon_i$$

$$\begin{array}{rcl} \displaystyle
   \sum_{j=1}^{i-1} r_j\epsilon_j & = & \displaystyle\sum_{j=1}^{i-1} \alpha_j^x \epsilon_j - \gamma^{\mbox{\scriptsize\em old}}
      \times \frac{|\texttt{constraintNormalization}|}{2} \times \epsilon_c^{\mbox{\scriptsize\em old}}\\ \displaystyle
   \sum_{j=1}^i r_j\epsilon_j & = & \displaystyle\sum_{j=1}^i \alpha_j^x \epsilon_j - \gamma^{\mbox{\scriptsize\em new}}
      \times \frac{|\texttt{constraintNormalization}|}{2} \times \epsilon_c^{\mbox{\scriptsize\em new}}\\
%  & = & \displaystyle\sum_{j=1}^i \alpha_j^x \epsilon_j - \gamma^{\mbox{\scriptsize\em new}}
%     \times \frac{|\texttt{constraintNormalization}|}{2} \times \left(\epsilon_c^{\mbox{\scriptsize\em old}} + \beta_i^y \times \frac{2}{\texttt{constraintNormalization}}\right)\\
   & = & \displaystyle\sum_{j=1}^i \alpha_j^x \epsilon_j - \gamma^{\mbox{\scriptsize\em new}}
      \times \frac{|\texttt{constraintNormalization}|}{2} \times \epsilon_c^{\mbox{\scriptsize\em old}} - \gamma^{\mbox{\scriptsize\em new}} \times \beta_i^y \times \epsilon_i \times \texttt{isNegative}
\end{array}$$

$$\beta_i^y = 0$$
$$\gamma^{\mbox{\scriptsize\em new}} \leftarrow \gamma^{\mbox{\scriptsize\em old}}$$
$$r_i = \alpha_i^x$$
$$\texttt{remainder}^{\mbox{\scriptsize\em new}} \leftarrow \texttt{remainder}^{\mbox{\scriptsize\em old}} + |\alpha_i^x|$$
$$\texttt{weightPositive}^{\mbox{\scriptsize\em new}} \leftarrow \texttt{weightPositive}^{\mbox{\scriptsize\em old}}$$
$$\texttt{match}^{\mbox{\scriptsize\em new}} = \texttt{matchPositive}^{\mbox{\scriptsize\em new}} \leftarrow \texttt{match}^{\mbox{\scriptsize\em old}}$$
$$\texttt{matchNegative}^{\mbox{\scriptsize\em new}} \leftarrow \texttt{matchNegative}^{\mbox{\scriptsize\em old}}$$

$$|\beta_i^y| < \texttt{DMinWeight} \times \texttt{weightAll}$$
$$\gamma^{\mbox{\scriptsize\em new}} \leftarrow \gamma^{\mbox{\scriptsize\em old}} = \gamma$$
$$r_i = \alpha_i^x - \gamma \times \beta_i^y \times \texttt{isNegative}$$
$$\texttt{remainder}^{\mbox{\scriptsize\em new}} \leftarrow \texttt{remainder}^{\mbox{\scriptsize\em old}} + |\alpha_i^x - \gamma \times \beta_i^y \times \texttt{isNegative}|$$
$$\texttt{weightPositive}^{\mbox{\scriptsize\em new}} \leftarrow \texttt{weightPositive}^{\mbox{\scriptsize\em old}}$$
$$\texttt{match}^{\mbox{\scriptsize\em new}} = \texttt{matchPositive}^{\mbox{\scriptsize\em new}} \leftarrow \texttt{match}^{\mbox{\scriptsize\em old}}$$
$$\texttt{matchNegative}^{\mbox{\scriptsize\em new}} \leftarrow \texttt{matchNegative}^{\mbox{\scriptsize\em old}} + |\gamma \times \beta_i^y|$$


$$\texttt{weightAll}{\mbox{\scriptsize\em old}} < \texttt{DMinWeight} \times |\beta_i^y|$$
$$\gamma^{\mbox{\scriptsize\em new}} \leftarrow \frac{\alpha_i^x}{\beta_i^y}$$
$$\begin{array}{rcl} \displaystyle
   \sum_{j=1}^{i-1} r_j^{\mbox{\scriptsize\em old}} \epsilon_j & = & \displaystyle\sum_{j=1}^{i-1} \alpha_j^x \epsilon_j - \gamma^{\mbox{\scriptsize\em old}}
      \times \frac{|\texttt{constraintNormalization}|}{2} \times \epsilon_c^{\mbox{\scriptsize\em old}}\\ \displaystyle
   \sum_{j=1}^i r_j^{\mbox{\scriptsize\em new}}\epsilon_j & = & \displaystyle\sum_{j=1}^i \alpha_j^x \epsilon_j - \gamma^{\mbox{\scriptsize\em new}}
      \times \frac{|\texttt{constraintNormalization}|}{2} \times \epsilon_c^{\mbox{\scriptsize\em old}}
      - \gamma^{\mbox{\scriptsize\em new}} \times \beta_i^y \times \epsilon_i \times \texttt{isNegative}\\
   & = & \displaystyle\sum_{j=1}^{i-1} \alpha_j^x \epsilon_j + \left(\alpha_i^x - \gamma^{\mbox{\scriptsize\em new}} \times \beta_i^y\times \texttt{isNegative}\right)\epsilon_i
      - \gamma^{\mbox{\scriptsize\em new}} \times \frac{|\texttt{constraintNormalization}|}{2} \times \epsilon_c^{\mbox{\scriptsize\em old}}\\
   & = & \displaystyle\sum_{j=1}^{i-1} \alpha_j^x \epsilon_j + \left(\alpha_i^x - \gamma^{\mbox{\scriptsize\em new}} \times \beta_i^y\times \texttt{isNegative}\right)\epsilon_i
      - \gamma^{\mbox{\scriptsize\em new}} \times \left(\sum_{j=1}^{i-1} \beta_j^y\epsilon_j\right)
\end{array}$$

$$\begin{array}{rcl} \displaystyle
\sum_{j=1}^i |r_j^{\mbox{\scriptsize\em new}}| & = & \displaystyle
   |\texttt{matchAll}^{\mbox{\scriptsize\em old}} - \gamma^{\mbox{\scriptsize\em new}} \times \texttt{weightPositive}^{\mbox{\scriptsize\em old}}|\\
   & & \hspace*{1in} + \gamma^{\mbox{\scriptsize\em new}} \times \left(\texttt{weightAll}^{\mbox{\scriptsize\em old}} - \texttt{weightPositive}^{\mbox{\scriptsize\em old}}\right)
\end{array}$$
$$\begin{array}{rcl} \displaystyle
\texttt{remainder}^{\mbox{\scriptsize\em new}} & \leftarrow & \displaystyle
   |\texttt{matchAll}^{\mbox{\scriptsize\em old}} - \gamma^{\mbox{\scriptsize\em new}} \times \texttt{weightPositive}^{\mbox{\scriptsize\em old}}|\\
    & & \hspace*{1in} + \gamma^{\mbox{\scriptsize\em new}} \times \left(\texttt{weightAll}^{\mbox{\scriptsize\em old}} - \texttt{weightPositive}^{\mbox{\scriptsize\em old}}\right)
\end{array}$$
$$\texttt{weightPositive}^{\mbox{\scriptsize\em new}} \leftarrow |\beta_i|$$
$$\texttt{match}^{\mbox{\scriptsize\em new}} = \texttt{matchPositive}^{\mbox{\scriptsize\em new}} \leftarrow |\gamma^{\mbox{\scriptsize\em new}} \times \beta_i|$$
$$\texttt{matchNegative}^{\mbox{\scriptsize\em new}} \leftarrow |\gamma^{\mbox{\scriptsize\em new}}| \times \left(\beta_i - \texttt{weightAll}^{\mbox{\scriptsize\em old}}\right)$$


$$\alpha_i^x = 0$$
$$r_i = - \gamma^{\mbox{\scriptsize\em new}} \times \beta_i^y \times \texttt{isNegative}$$
$$\texttt{remainder}^{\mbox{\scriptsize\em new}} \leftarrow \texttt{remainder}^{\mbox{\scriptsize\em old}} + |\gamma^{\mbox{\scriptsize\em old}} \times \beta_i^y|$$
$$\texttt{weightPositive}^{\mbox{\scriptsize\em new}} \leftarrow \texttt{weightPositive}^{\mbox{\scriptsize\em old}}$$
$$\texttt{match}^{\mbox{\scriptsize\em new}} = \texttt{matchPositive}^{\mbox{\scriptsize\em new}} \leftarrow \texttt{match}^{\mbox{\scriptsize\em old}}$$
$$\texttt{matchNegative}^{\mbox{\scriptsize\em new}} \leftarrow \texttt{matchNegative}^{\mbox{\scriptsize\em old}} + |\gamma^{\mbox{\scriptsize\em old}} \times \beta_i^y|$$


$$\gamma^{\mbox{\scriptsize\em old}} = 0 \texttt{ and } |\beta_i^y| \geq \texttt{DMinWeight} \times \texttt{weightAll} \texttt{ and } \texttt{weightAll} \geq \texttt{DMinWeight} \times |\beta_i^y|$$
$$\gamma^{\mbox{\scriptsize\em new}} \leftarrow 0$$
$$r_i = \alpha_i^x$$
$$\texttt{remainder}^{\mbox{\scriptsize\em new}} \leftarrow \texttt{remainder}^{\mbox{\scriptsize\em old}} + |\alpha_i^x|$$
$$\texttt{weightPositive}^{\mbox{\scriptsize\em new}} \leftarrow \texttt{weightPositive}^{\mbox{\scriptsize\em old}}$$
$$\texttt{match}^{\mbox{\scriptsize\em new}} = \texttt{matchPositive}^{\mbox{\scriptsize\em new}} \leftarrow \texttt{match}^{\mbox{\scriptsize\em old}} = 0$$
$$\texttt{matchNegative}^{\mbox{\scriptsize\em new}} \leftarrow \texttt{matchNegative}^{\mbox{\scriptsize\em old}} + |\gamma \times \beta_i^y| = 0$$


$$\begin{array}{l} \displaystyle
\left|\frac{\alpha_i^x}{\beta_i^y}\right| < |\gamma^{\mbox{\scriptsize\em old}}| \texttt{ and } \gamma^{\mbox{\scriptsize\em old}} \neq 0 \texttt{ and } \texttt{sign}\left(\frac{\alpha_i^x}{\beta_i^y}\right) = \texttt{sign}(\gamma)\\
\hspace*{1in} \texttt{and } |\beta_i^y| \geq \texttt{DMinWeight} \times \texttt{weightAll} \texttt{ and } \texttt{weightAll} \geq \texttt{DMinWeight} \times |\beta_i^y|
\end{array}$$

$$\gamma^{\mbox{\scriptsize\em new}} \leftarrow \frac{\alpha_i^x}{\beta_i^y}$$

$$\begin{array}{rcl} \displaystyle
   \sum_{j=1}^{i-1} r_j^{\mbox{\scriptsize\em old}} \epsilon_j & = & \displaystyle\sum_{j=1}^{i-1} \alpha_j^x \epsilon_j - \gamma^{\mbox{\scriptsize\em old}}
      \times \frac{|\texttt{constraintNormalization}|}{2} \times \epsilon_c^{\mbox{\scriptsize\em old}}\\ \displaystyle
   \sum_{j=1}^i r_j^{\mbox{\scriptsize\em new}}\epsilon_j & = & \displaystyle\sum_{j=1}^i \alpha_j^x \epsilon_j - \gamma^{\mbox{\scriptsize\em new}}
      \times \frac{|\texttt{constraintNormalization}|}{2} \times \epsilon_c^{\mbox{\scriptsize\em old}}
      - \gamma^{\mbox{\scriptsize\em new}} \times \beta_i^y \times \epsilon_i \times \texttt{isNegative}\\
   & = & \displaystyle\sum_{j=1}^i \alpha_j^x \epsilon_j + \frac{\gamma^{\mbox{\scriptsize\em new}}}{\gamma^{\mbox{\scriptsize\em old}}}
      \times \left(\sum_{j=1}^{i-1} r_j^{\mbox{\scriptsize\em old}}\epsilon_j - \sum_{j=1}^{i-1} \alpha_j^x \epsilon_j\right)
      - \gamma^{\mbox{\scriptsize\em new}} \times \beta_i^y \times \epsilon_i \times \texttt{isNegative}\\
   & = & \displaystyle\left(1 - \frac{\gamma^{\mbox{\scriptsize\em new}}}{\gamma^{\mbox{\scriptsize\em old}}}\right)\times \sum_{j=1}^{i-1} \alpha_j^x \epsilon_j
      + \left(\alpha_i^x - \gamma^{\mbox{\scriptsize\em new}} \times \beta_i^y \times \texttt{isNegative}\right)\epsilon_i
      + \frac{\gamma^{\mbox{\scriptsize\em new}}}{\gamma^{\mbox{\scriptsize\em old}}} \times \left(\sum_{j=1}^{i-1} r_j^{\mbox{\scriptsize\em old}}\epsilon_j\right)
\end{array}$$

$$
   \sum_{j=1}^i |r_j^{\mbox{\scriptsize\em new}}| = \left(1 - \frac{\gamma^{\mbox{\scriptsize\em new}}}{\gamma^{\mbox{\scriptsize\em old}}}\right)
      \times \sum_{j=1}^{i-1} |\alpha_j^x|
      + \left|\alpha_i^x - \gamma^{\mbox{\scriptsize\em new}} \times \beta_i^y \times \texttt{isNegative}\right|
      + \frac{\gamma^{\mbox{\scriptsize\em new}}}{\gamma^{\mbox{\scriptsize\em old}}} \times \left(\sum_{j=1}^{i-1} |r_j^{\mbox{\scriptsize\em old}}|\right)$$

$$ \texttt{remainder}^{\mbox{\scriptsize\em new}} \leftarrow \left(1 - \frac{\gamma^{\mbox{\scriptsize\em new}}}{\gamma^{\mbox{\scriptsize\em old}}}\right)
      \times \texttt{matchAll}^{\mbox{\scriptsize\em old}}
      + \left|\alpha_i^x - \gamma^{\mbox{\scriptsize\em new}} \times \beta_i^y \times \texttt{isNegative}\right|
      + \frac{\gamma^{\mbox{\scriptsize\em new}}}{\gamma^{\mbox{\scriptsize\em old}}} \times \texttt{remainder}^{\mbox{\scriptsize\em old}}
$$
$$ \texttt{remainder}^{\mbox{\scriptsize\em new}} \leftarrow \left(1 - \frac{\gamma^{\mbox{\scriptsize\em new}}}{\gamma^{\mbox{\scriptsize\em old}}}\right)
      \times \texttt{matchAll}^{\mbox{\scriptsize\em old}}
      + \frac{\gamma^{\mbox{\scriptsize\em new}}}{\gamma^{\mbox{\scriptsize\em old}}} \times \texttt{remainder}^{\mbox{\scriptsize\em old}}
$$
$$\texttt{weightPositive}^{\mbox{\scriptsize\em new}} \leftarrow \texttt{weightPositive}^{\mbox{\scriptsize\em old}} + |\beta_i|$$

$$\begin{array}{rcl} \displaystyle
   \texttt{match}^{\mbox{\scriptsize\em new}} & = & \displaystyle \sum_{j=1}^n |\gamma^{\mbox{\scriptsize\em new}} \times \beta_j| \textrm{ if }
      \texttt{sign}(\gamma^{\mbox{\scriptsize\em new}} \times \beta_j^y) = \texttt{sign}(\alpha_j^x)\\
   & = & \displaystyle \frac{\gamma^{\mbox{\scriptsize\em new}}}{\gamma^{\mbox{\scriptsize\em old}}} \times \sum_{j=1}^{i-1}\left(|\gamma^{\mbox{\scriptsize\em old}} \times \beta_j| \textrm{ if }
      \texttt{sign}(\gamma^{\mbox{\scriptsize\em old}} \times \beta_j^y) = \texttt{sign}(\alpha_j^x)\right) + |\gamma^{\mbox{\scriptsize\em new}} \times \beta_i|\\
   & \leftarrow & \displaystyle\frac{\gamma^{\mbox{\scriptsize\em new}}}{\gamma^{\mbox{\scriptsize\em old}}} \times \texttt{match}^{\mbox{\scriptsize\em old}} + |\alpha_i|
\end{array}$$

$$\begin{array}{l} \displaystyle
\left|\frac{\alpha_i^x}{\beta_i^y}\right| \geq |\gamma^{\mbox{\scriptsize\em old}}| \texttt{ and } \gamma^{\mbox{\scriptsize\em old}} \neq 0 \texttt{ and } \texttt{sign}\left(\frac{\alpha_i^x}{\beta_i^y}\right) = \texttt{sign}(\gamma)\\
\hspace*{1in} \texttt{and } |\beta_i^y| \geq \texttt{DMinWeight} \times \texttt{weightAll} \texttt{ and } \texttt{weightAll} \geq \texttt{DMinWeight} \times |\beta_i^y|
\end{array}$$
$$\gamma^{\mbox{\scriptsize\em new}} \leftarrow \gamma^{\mbox{\scriptsize\em old}} = \gamma$$
$$r_i = \alpha_i^x - \gamma \times \beta_i^y \times \texttt{isNegative}$$
$$\texttt{remainder}^{\mbox{\scriptsize\em new}} \leftarrow \texttt{remainder}^{\mbox{\scriptsize\em old}} + |\alpha_i^x - \gamma \times \beta_i^y \times \texttt{isNegative}|$$
$$\texttt{weightPositive}^{\mbox{\scriptsize\em new}} \leftarrow \texttt{weightPositive}^{\mbox{\scriptsize\em old}} + |\beta_i^y|$$
$$\texttt{match}^{\mbox{\scriptsize\em new}} = \texttt{matchPositive}^{\mbox{\scriptsize\em new}} \leftarrow \texttt{match}^{\mbox{\scriptsize\em old}} + |\gamma \times \beta_i^y|$$
$$\texttt{matchNegative}^{\mbox{\scriptsize\em new}} \leftarrow \texttt{matchNegative}^{\mbox{\scriptsize\em old}}$$


$$\begin{array}{l} \displaystyle
\texttt{sign}\left(\frac{\alpha_i^x}{\beta_i^y}\right) = -\texttt{sign}(\gamma^{\mbox{\scriptsize\em old}}) \texttt{ and } \gamma^{\mbox{\scriptsize\em old}} \neq 0\\
\hspace*{1in} \texttt{and } |\beta_i^y| \geq \texttt{DMinWeight} \times \texttt{weightAll} \texttt{ and } \texttt{weightAll} \geq \texttt{DMinWeight} \times |\beta_i^y|
\end{array}$$
$$\gamma^{\mbox{\scriptsize\em new}} \leftarrow 0$$
$$\sum_{j=1}^{i} r_j\epsilon_j = \sum_{j=1}^{i} \alpha_j^x\epsilon_j$$
$$\texttt{remainder}^{\mbox{\scriptsize\em new}} \leftarrow \texttt{matchAll}^{\mbox{\scriptsize\em old}} + |\alpha_i^x|$$
$$\texttt{weightPositive}^{\mbox{\scriptsize\em new}} \leftarrow 0$$
$$\texttt{match}^{\mbox{\scriptsize\em new}} = \texttt{matchPositive}^{\mbox{\scriptsize\em new}} \leftarrow 0$$
$$\texttt{matchNegative}^{\mbox{\scriptsize\em new}} \leftarrow 0$$


$$\epsilon_c = \frac{2\times\widehat{y} - \texttt{constraintNormalization}}{\texttt{constraintNormalization}}$$

$$\widehat{x} = \gamma \times \widehat{y} + \left(\widehat{x} - \gamma \times \widehat{y}\right)$$

$$\widehat{x} = \gamma \times \frac{|\texttt{constraintNormalization}|}{2} \times \epsilon_c + \left(\widehat{x} - \gamma \times \widehat{y}\right) + \gamma \times \frac{|\texttt{constraintNormalization}|}{2}$$



$$\beta_0^y + \sum_{i=1}^n \beta_i^y \epsilon_i \geq 0$$
$$\beta_0^y + \sum_{i=1}^n \beta_i^y \epsilon_i \leq 0$$

$$\beta_i^y \in \mathbb{R}$$

$$\beta_0^y \leq 0$$
$$\sum_{i=1}^n |\beta_i^y| \geq -\beta_0^y$$

$$\beta_0^y \geq 0$$
$$\sum_{i=1}^n |\beta_i^y| \geq \beta_0^y$$

$$\beta_0^y < 0$$
$$\sum_{i=1}^n |\beta_i^y| < -\beta_0^y$$
$$\sum_{i=1}^n |\beta_i^y| \leq -\beta_0^y$$

$$\beta_0^y > 0$$
$$\sum_{i=1}^n |\beta_i^y| < \beta_0^y$$
$$\sum_{i=1}^n |\beta_i^y| \leq \beta_0^y$$

$$\epsilon_c = \frac{\displaystyle\left(\beta_0^y + \sum_{i=1}^n |\beta_i^y|\right) +  \sum_{i=1}^n 2 \beta_i^y \epsilon_i}
   {\displaystyle\beta_0^y - \sum_{i=1}^n |\beta_i^y|}$$
$$\left[\beta_0^y - \sum_{i=1}^n |\beta_i^y|, 0\right]$$

$$\epsilon_c = \frac{\displaystyle\left(\beta_0^y - \sum_{i=1}^n |\beta_i^y|\right) +  \sum_{i=1}^n 2 \beta_i^y \epsilon_i}
   {\displaystyle\beta_0^y + \sum_{i=1}^n |\beta_i^y|}$$
$$\left[0, \beta_0^y + \sum_{i=1}^n |\beta_i^y|\right]$$

$$\lambda_i \epsilon_i$$
$$\lambda_0$$
$$\texttt{fNegative} = \texttt{true} \Rightarrow (\epsilon_c-1) \times \texttt{dNormalization} = \texttt{dConstant} + \sum_{i = 1}^n \lambda_i \epsilon_i \leq 0$$
$$\texttt{fNegative} = \texttt{false} \Rightarrow (\epsilon_c+1) \times \texttt{dNormalization} = \texttt{dConstant} + \sum_{i = 1}^n \lambda_i \epsilon_i \geq 0$$

$$\texttt{fNegative} = \texttt{true} \Rightarrow \texttt{dNormalization} = \frac{\displaystyle\sum_{i = 1}^n |\lambda_i| - \texttt{dConstant}}{2}$$
$$\texttt{fNegative} = \texttt{false} \Rightarrow \texttt{dNormalization} = \frac{\displaystyle\sum_{i = 1}^n |\lambda_i| + \texttt{dConstant}}{2}$$

$$\lambda_0 + \sum_{i = 1}^n \lambda_i \epsilon_i$$

$$\texttt{fNegative} = \texttt{true} \Rightarrow \epsilon_c = \frac{\displaystyle \lambda_0 + \sum_{i = 1}^n |\lambda_i| + 2 \lambda_i \epsilon_i}
   {\displaystyle \sum_{i = 1}^n |\lambda_i| - \lambda_0}$$
$$\texttt{fNegative} = \texttt{false} \Rightarrow \epsilon_c = \frac{\displaystyle \lambda_0 + \sum_{i = 1}^n 2 \lambda_i \epsilon_i - |\lambda_i|}
   {\displaystyle\sum_{i = 1}^n |\lambda_i| + \lambda_0}$$

$$\texttt{isNegative} = \texttt{true} \Rightarrow \texttt{dConstant} + \sum_{i = 1}^n \lambda_i \epsilon_i \leq 0$$
$$\texttt{isNegative} = \texttt{false} \Rightarrow \texttt{dConstant} + \sum_{i = 1}^n \lambda_i \epsilon_i \geq 0$$

$$\forall i \in [1, n]. \, \lambda_i = \alpha_i^x$$

$$\texttt{\bf this} = \texttt{dConstant} + \sum_{i = 1, i \neq k}^n \lambda_i \epsilon_i + \lambda_k \epsilon_k = \texttt{remainder} + \lambda_k \epsilon_k$$
$$\texttt{\tt remainder} = \texttt{dConstant} + \sum_{i = 1, i \neq k}^n \lambda_i \epsilon_i$$
$$\texttt{fNegative} = \texttt{true} \Rightarrow \texttt{remainder} + \lambda_k \epsilon_k  = \texttt{constraintResult} \leq 0$$
$$\texttt{fNegative} = \texttt{false} \Rightarrow \texttt{remainder} + \lambda_k \epsilon_k  = \texttt{constraintResult} \geq 0$$
$$\texttt{remainder} + \lambda_k \epsilon_k^{\mbox{\scriptsize\em new}} = 0$$
$$\lambda_k(\epsilon_k - \epsilon_k^{\mbox{\scriptsize\em new}}) = \texttt{constraintResult}$$ 
$$(\epsilon_k - \epsilon_k^{\mbox{\scriptsize\em new}}) = \frac{\texttt{constraintResult}}{\lambda_k}$$ 
$$\epsilon_k^{\mbox{\scriptsize\em new}} - \epsilon_k$$

$$\epsilon_c^2 = 1 + \frac{1}{3}(-2 + 2.5 \epsilon_1 + \epsilon_3 - 0.5 \epsilon_4)$$
$$\epsilon_1 = \frac{1}{2.5}\left((-1 \times 3 + 2) - \epsilon_3 + 0.5 \epsilon_4 + 3 \epsilon_c^2\right)$$
$$\epsilon_1 = -0.4 - 0.4 \epsilon_3 + 0.2 \epsilon_4 + 1.2 \epsilon_c^2$$
$$2.5(\epsilon_1-1) = -3.5 - \epsilon_3 + 0.5 \epsilon_4 + 3 \epsilon_c^2 \leq 0$$
$$\begin{array}{rcl}
\texttt{sabmObjective} & = & \displaystyle 1.5 \epsilon_1 - 2 \epsilon_2 + 0.5 \epsilon_3 - 0.75 \epsilon_4\\
& = & \displaystyle 1.5 \left(\frac{1}{2.5}\left((-1 \times 3 + 2) - \epsilon_3 + 0.5 \epsilon_4 + 3 \epsilon_c^2\right)\right)- 2 \epsilon_2 + 0.5 \epsilon_3 - 0.75 \epsilon_4\\
& = & \displaystyle 1.5 \left(-0.4 - 0.4 \epsilon_3 + 0.2 \epsilon_4 + 1.2 \epsilon_c^2\right) - 2 \epsilon_2 + 0.5 \epsilon_3 - 0.75 \epsilon_4\\
& = & \displaystyle -0.6 -2 \epsilon_2 - 0.1 \epsilon_3 - 0.45 \epsilon_4 + 1.8 \epsilon_c^2
\end{array}$$

$$\begin{array}{l} \displaystyle
  \texttt{equation.isNegative()} = \texttt{true} \Rightarrow (\epsilon_k-1) \times \texttt{equation.getNormalization()}\\
      \displaystyle \hspace*{3in} = \texttt{equation.getConstant()} + \sum_{i = 1}^n \lambda_i \epsilon_i \leq 0
\end{array}$$
$$\begin{array}{l} \displaystyle
  \texttt{equation.isNegative()} = \texttt{false} \Rightarrow (\epsilon_k+1) \times \texttt{equation.getNormalization()}\\
      \displaystyle \hspace*{3in} = \texttt{equation.getConstant()} + \sum_{i = 1}^n \lambda_i \epsilon_i \geq 0
\end{array}$$


\end{document}

